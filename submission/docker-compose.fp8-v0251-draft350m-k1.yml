# Diagnostic only. This preserves the configuration that completed SER8
# locally: k=1, eager execution, one active sequence, and prefix caching off.
# The public 350M draft had only 10.39% acceptance locally, so a low ERS is
# expected. Do not use this as a new baseline.
services:
  model:
    image: siconhoccode/lfm-serving:fp8-v0251-draft350m-k1-v1
    entrypoint:
      - python3
      - -m
      - vllm.entrypoints.openai.api_server
    command:
      - --model=/model
      - --served-model-name=LFM2.5-1.2B-Instruct
      - --host=0.0.0.0
      - --port=8000
      - --dtype=bfloat16
      - --quantization=fp8_per_tensor
      - --max-model-len=5120
      - --gpu-memory-utilization=0.70
      - --tensor-parallel-size=1
      - --max-num-seqs=1
      - --max-num-batched-tokens=512
      - --no-enable-prefix-caching
      - --enforce-eager
      - --no-async-scheduling
      - --speculative-config={"method":"draft_model","model":"/draft-model","num_speculative_tokens":1,"draft_tensor_parallel_size":1}
      - --disable-log-stats
      - --disable-uvicorn-access-log
    ports:
      - "8000:8000"
    shm_size: "2g"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
